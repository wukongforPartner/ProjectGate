# ProjectGate Alpha 快速开始

1. 把项目文档放进 `ProjectDocsInbox` 文件夹。
2. 运行 `install_projectgate_alpha.py` 初始化 ProjectGate 工作区。
3. 从文档编译 Project Pack。
4. 从 Project Pack 构建 Codex Pack。
5. 安装 Codex Pack。
6. 在 Codex 中使用 `/goal $projectgate -p L: ...`。

Profile：

- `-p L`：低成本
- `-p M`：中等深度
- `-p H`：深度审查
